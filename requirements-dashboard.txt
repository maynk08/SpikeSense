# Dependencies for the single-service Streamlit Community Cloud deployment.
# The dashboard scores the Isolation Forest + LSTM models in-process
# (dashboard/local_scoring.py), so TensorFlow is required here. No FastAPI —
# there is no separate backend in this deployment.

numpy==1.26.4
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
joblib==1.5.3
tensorflow-cpu==2.16.1
streamlit==1.42.2
plotly==5.22.0
requests==2.33.1
pyyaml==6.0.1
